import type React from 'react';

export const W = 300, H = 80, PX = 16, PY = 10;
export const pw = W - PX * 2;
export const ph = H - PY * 2;

export const weekChartStyles = {
  svg: { width: '100%' } satisfies React.CSSProperties,
};
